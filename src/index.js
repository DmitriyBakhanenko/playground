import React from 'react';
import ReactDOM from 'react-dom';
import App from './lessons/youtube/components/App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
